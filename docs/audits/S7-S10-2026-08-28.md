# S7~S10 实现与质量复审 — 2026-08-28

> 分支 `codex/github-actions` @ `5b4481e` · 探针 17 个并行沙盒走查（含迟到 S9 探针 a3d3683） + `go vet` 0 + `go test -count=1 ./internal/...` 33 包绿（本轮新增 9 个探针：S7-wiring / S7-resilience / S8×2 / S9×2 / S10 / Runtime / Complexity / Fluency；叠加上一轮 8 个探针产物做去重）

## 1. 分级总览

| 维度 | 判定 | 一句话 |
|---|---|---|
| **Wiring（是否真埋入运行路径）** | **PASS（带 4 高）** | S7 CAS/seal/clean、S8 intake/CAS 不可变、S9 exact-set/authority、S10 gate/envelope 均在主路径 fail-closed；残留高风险集中在“证据内容层零校验”与 journal 轮转崩溃窗口 |
| **复杂度↔收益** | **未越峰值，但 RC-10 已二次爬坡** | RC-11/12 六项高收益低复杂度；RC-10 三项增配、一条未做，净收敛为负 |
| **Fluency（操作流畅）** | **7.8/10 · FRICTION 11 / BLOCKED 0** | 主路径可用，S8 20 动词/长错误面为主要摩擦源；S10 envelope 最后一公里无脚手架 |
| **Resilience（错误路径兜底）** | **RESILIENT 29 / LEAKY 3 / SILENT 0** | stale-revision / 指纹漂移 / 守卫存根 / metrics 均可观测或 fail-closed；3 个 LEAKY 为并发 seal 悬空、phantom evidence、journal 崩溃窗口 |

**去重后 findings：46（高 15 / 中 19 / 低 12）**，按 `file:line:title` 去重，跨 17 探针重复度约 30%。对抗式三视角复核：保留 40 / 降级 6（见 §7）。迟到 S9 探针（a3d3683）补 7 条，已并入 §3.4 与证据 JSON。

## 2. 四象限裁决

| 象限 | 结论 | 证据锚点 |
|---|---|---|
| WIRING | 7 成以上真实；3 成“结构门而非证据门” | S7 `submit.go:380` CAS / `guards.go:411` exact-set；S8 `intake.go:88` sha 链；S9 `repair/artifact.go:114` O_EXCL + `runtime.go:660` exact-set；S10 `qualitygate/evaluator.go:431` 重哈希。反例见 §3 高 |
| 复杂度-收益 | RC-11/12 ROI 最高；RC-10 最差 | `fingerprint.go:50` 单消费者零测试；`metrics/s7.go:241` 管增长不管数量；`submit.go:67` 巨事务未拆 |
| Fluency | 主路径自解释，边缘面摩擦集中 | `s7_status.go:302 --explain` / `investigation_command.go:844 --emit-template` 为正例；`intake.go:232` 循环 next / `s10_command.go:92` envelope 无示例为反例 |
| Resilience | 机制兜底真实，但两处“恢复器自毁” | `store.go:1800` 四 marker 顺序正确；`store.go:2657` 轮转恢复丢弃 marker 导致永久非连续（HIGH-1）；`case_workflow.go:136` phantom evidence 绕过新鲜度门 |

## 3. 按域 findings（去重后，按 severity 排序）

### 3.1 Runtime 控制面（L4）

| # | 级别 | 标题 | 位置 | Wiring | 摘要 |
|---|---|---|---|---|---|
| R-H1 | **高** | journal 轮转崩溃窗口：marker 被静默清除，combined 永久非连续 | `internal/runtime/store.go:2657-2691, 2548-2589` | GAP | `maybeRotateJournalLocked` 序列 marker→archive→truncate→clear；若在 archive 已写、truncate 前崩溃，恢复路径用 `inspectJournal` 合并段+active 报 non-contiguous 后仍 `clear marker + return nil`，此后每次 `Update/Snapshot` 永久失败且报告“成功”；无测试覆盖 |
| R-M2 | 中 | rotation 恢复校验失败被吞 `_ = err` | `store.go:2581-2584` | LEAKY | `validateStateJournalPair` 失败丢弃后照常清 marker，违反“半完成必须可重建”可观测性 |
| R-M3 | 中 | recovery 白名单缺 `journal-rotation-pending` | `internal/runtime/recovery.go:757` | GAP | `knownRecoverySourcePendingPaths` 仅列 3 marker；plan 驱动 recovery 原子替换 state+journal 后 rotation TailSequence/SHA 绑定失效无感知 |
| R-M4 | 中 | metrics 写入非原子 + 锁无 owner/无回收 | `internal/metrics/metrics.go:243, 469` | LEAKY | `os.WriteFile` 直接覆盖，半写截断后 `Read` 永久失败；`acquireLock` 无 owner、无 stale 回收，崩溃即锁泄漏 |
| R-L5 | 低 | `extractArchiveSeq` 非数字段名返回 0 | `store.go:2886` | - | 损坏名排最前致 fail-closed，安全但报错失真；归档段无 purge（登记为已知边界） |
| R-L6 | 低 | 指纹畸形行静默跳过 | `fingerprint.go:94` | - | 缺 path/sha 行不投毒但与“缺席”不可区分，三元不能区分不完整注册 |
| R-L7 | 低 | `ComputeRevisionPair` revision=0 混同缺席 | `fingerprint.go:71` | - | 刚 bind 的 0 与无 revision 同值，仅影响观测 |

对照：CAS `store.go:1277`、`recoverPendingWritesLocked:1800` 四 marker 顺序、段感知 `journalSegmentPaths:2874`、指纹三元 `fingerprint.go`、59/48 vs 58/33 superset、`1.1.0` 均实测 WIRING_OK。

### 3.2 S7 审查轮次

| # | 级别 | 标题 | 位置 | 摘要 |
|---|---|---|---|---|
| S7-M1 | 中 | Submit 单 CAS 内含 seal/clean/pause/drain | `internal/review/submit.go:337, 413` | WIRING_OK：轮次消费者与结果消耗同一事务，P0 后抑制队列调度 |
| S7-M2 | 中 | 退出守卫为语义 exact-set 非证据存根 | `internal/transition/guards.go:411, verification/clean_round.go:47` | WIRING_OK：TR-008/009 精确集 + 7 检查重算 |
| S7-M3 | 中 | 调度波门 CAS 内强制 | `internal/assignment/review_bind.go:29, internal/review/plan.go:113` | WIRING_OK：static→behavior、claim DAG、资源锁排队、overlap/cold-start/N-A checklist 均在注册门内 |
| S7-L1 | 低 | `RecordS7ResultSubmit` 不覆盖 site-lost blocker | `internal/review/blocked.go:486` | 可观测性缺口，非控制流绕过 |
| S7-L2 | 低 | 39 guard / 23 action 未声明残留无反向 drift 检测 | `internal/transition/catalog.go:527` | superset 设计正确，但历史残留不报错；已加 `guards.go:104` 禁新增 stub |

S7 总体 **PASS**，其余错误路径（stale revision 双层拦截、冻结基线双源 drift、evidence 类型/生产者独立性、Plan sha 校验）均为 RESILIENT，见 S7-resilience 探针全量矩阵。

### 3.3 S8 调查

| # | 级别 | 标题 | 位置 | 摘要 |
|---|---|---|---|---|
| S8-H1 | **高** | hypothesis/result `evidence_refs` 零存在性/哈希校验，幽灵证据可闭合因果并放行 `s9_repair` | `internal/investigation/case_workflow.go:136, 192, 435, 939` | GAP：仅 `nonEmptyStrings`；`evidenceFingerprint` 对任意新字符串即算“新证据”；`validateCausalClosure` 仅结构非空校验；对照 `validateEvidenceReferences:788` reassessment 侧有完整 SHA 校验，形成不对称 |
| S8-H2 | **高** | `failure_boundary_refs / cross_layer_trace / evidence_gaps` 永远空的占位字段 | `internal/investigation/intake.go:266` | THEATER：blueprint §2.3 三视图无任何读写方 |
| S8-H3 | **高** | `baseline_digest` 在 S8 永不重算，封批后基线漂移对 S8 不可见 | `intake.go:263` | intake 仅复制，`updateCaseRevision:649` 也不比对 |
| S8-M1 | 中 | `claim_coverage_summary / blocked_claims / investigation_readiness` S7 封存后 S8 零消费 | `internal/schema/assets/observation-batch.schema.json:73` | 单向数据，证据缺口视图不成立 |
| S8-M2 | 中 | ingest 缺 lifecycle/phase 门，可在 `observation_sealed` 前抢跑建 Case | `intake.go:68` | 无 `bug_resolution.investigation` 检查，指针可悬挂 |
| S8-M3 | 中 | `investigate_more` 仅字符串指纹新鲜度，无时间维度 | `case_workflow.go:435` | 与 H1 叠加，幽灵 ref 即可重入 |
| S8-L1 | 低 | `hypothesis.status` 死字段恒为 open | `case_workflow.go:166` | 展示层谎言，非安全洞 |
| S8-L2 | 低 | batch `conclusion:sealed` 非 required、Finding 工件 post-ingest 篡改不复检 | `observation-batch.schema.json:38, intake.go:46` | schema 不保证封批标记；后续动词不重读工件哈希 |
| S8-L3 | 低 | ingest `--emit-template` 在 CAS 后执行，失败返回非零可诱导重试 | `internal/cli/investigation_command.go:881` | 幂等 `ErrAlreadyIngested` 可自解释，风险低 |

正面：batch/finding sha 链、exact set、Case/Contract 不可变写与 revision_history、S9 `contract_approved` 单入口、定向失败回流 S8 重开时 retire 旧指针，均为 WIRING_OK。

### 3.4 S9 修复链

| # | 级别 | 标题 | 位置 | 摘要 |
|---|---|---|---|---|
| S9-H1 | **高** | TargetedReverification `pass` 断言零证据校验，可零证据伪造 PASS | `internal/repair/evidence.go:151, 184` | `validateTargetedFailureEvidence` 仅非 pass 要求 evidence；`ValidateTargetedReverification` 仅 exact slot 覆盖，无 evidence 非空/执行校验 |
| S9-H5 | **高** | RepairContract `approval_hash` 只写不验、`approved_by` 无权威校验、批准不经 `human_boundary` | `internal/investigation/contract.go:158, 57, 222` | 仅查非空，不与 draft 重算；Actor=`orchestrator` 不走 `human_decision` scope，自签即 `approved` |
| S9-H6 | **高** | `max_full_review_rounds` 预算被 `CommitRepairHandoff` 直写 `TR-012` 绕过 | `internal/repair/runtime.go:1140, 1144` | 预算门仅在 `transition` 引擎 `actionStartReviewRound`，handoff 用 Store 直写无条件 `round++` |
| S9-H7 | **高** | `RepairLimitError→GTR-004` 桥无生产调用方，超限不进 `paused` | `internal/adapter/dispatch.go:38, cli/run.go:1675` | `DispatchRepairLimitExceeded` 仅测试调用；生产 `bug-event` 仅打印错误，lifecycle 不暂停 |
| S9-H8 | **高** | Case/Contract 主路径无重试预算，`investigate_more↔s9_repair` 可无限循环 | `internal/investigation/case_workflow.go:323` | `attempt_count` 仅 legacy BUG 路径递增，主链无 `max_attempts` |
| S9-M5 | **高** | 独立验证者身份可被未认领兄弟 Assignment 穿透，同代理自验证 | `internal/repair/runtime.go:737, 740, 774` | `owner` 空的兄弟 Assignment 合法；`dispatchedVerifierAgentID` 空与 owner 不等不拦 |
| S9-M1 | 中 | 独立 verifier 池 = 同 plan builder 集合，独立性退化为“不同 assignment” | `internal/repair/runtime.go:726` | 仍持产品写权限，role-family 无区分，可互验（与 M5 叠加） |
| S9-M2 | 中 | `decision=reverify` 不强制落入 `required_reverification_ids` | `internal/repair/evidence.go:30, runtime.go:589` | 生产侧与消费侧门均空转 |
| S9-M3 | 中 | `runtime transition --id TR-012` 可旁路 `CommitRepairHandoff` 原子事务 | `internal/cli/run.go:1047, internal/transition/engine.go:581` | 手动路径 guard 对空 entities 空转 + `evidenceBackedGuard` 任意 k=v 即过，绕过 `createS7ReviewPlanSeed / round++ / clean_round 清空` |
| S9-M4 | 中 | 合成 transition id 不在目录，白名单自证 | `internal/repair/runtime.go:1179` | `S9-*` id 非目录声明，审计回放不可解释 |
| S9-T1 | 中 | `same_contract_failure_count` 永为 0，`max_same_contract_failures` 死检查 | `internal/assignment/bug_lifecycle.go:363, 586` | 仅初始化 0，无自增，分支不可达 |
| S9-L1 | 低 | `authority-fingerprint` 缺失时静默放行 | `runtime.go:525` | 兼容旧 session，但可变键可被清掉永久关门 |
| S9-L2 | 低 | ChangeImpact scope 双向 `pathMatches` 过松 | `evidence.go:30` | 宽 scope 可过覆盖检查，但 diff 仍 exact-set 钉死，仅叙事损失 |
| S9-L3 | 低 | 红前检/blue-check 仅非空 token | `internal/repair/plan_report.go:53` | 结构门真实，内容不可证 |
| S9-L4 | 低 | `checkS9AuthorityFreshness` 同路径三次全仓哈希 | `runtime.go:630` | 冗余成本与 TOCTOU |
| S9-T2 | 低 | 影子字段：`RetainedEvidenceIDs`/`RecoveryEvidence`/`BaselineDigest`/`Session.Status`/单数版 `validateCurrentRepairResult` 零消费 | `internal/repair/model.go:238, 222, 64` | schema 强制但运行时不读；真实状态在 Runtime 指针 |

正面确认：Contract **哈希链与 changed-artifacts 精确集**在 result→impact→changeset→S7 全链真实闭合（`artifact.go:123 hash drift` / `runtime.go:660 exact-set` / `repair_baseline.go:148` 逐条消费）；**CAS revision 与 `human_decision` scope 串**也真实生效。但“权威批准 + 有限循环”承诺主要由文档而非运行时承担（H5-H8）。

### 3.5 S10 终审

| # | 级别 | 标题 | 位置 | 摘要 |
|---|---|---|---|---|
| S10-H1 | **高** | coverage 行 `evidence_refs` 仅存在性校验，可自引 S10 信封自证 | `internal/qualitygate/evaluator.go:548, internal/acceptance/manifest.go:282` | THEATER：任意 valid 当前代证据即满足，无 kind/内容约束；信封自身即 valid 证据 |
| S10-H2 | **高** | Gate 对 blocked 一律 `requireClean=true`，`ValidateForOutcomeWithBaseline` 死代码，诚实 blocked 永不通过 | `evaluator.go:437, manifest.go:201` | 注册接受 unknown 行 blocked，gate 却以 unknown 判 conflict；`s10 status` 指示走 TR-018，gate 拒，诱导粉饰 |
| S10-H3 | **高** | 外部分母在投影不可得时静默退化为自声明分母 | `internal/review/coverage.go:58, manifest.go:520, evaluator.go:475` | `Diagnostics` 非空→nil→baseline 空→免检；`baseline.go:70` 回退到 agent 可控 `scope_refs`，篡改 completion 信封即可使 changed_path 覆盖率虚高 |
| S10-M1 | 中 | `review_required` manifest 永不在 gate 重校验，注册后可篡改 | `internal/qualitygate/registry.go:230` | gate 仅挂三类 complete/blocked/approved，review_required 路由文件体无人重哈希 |
| S10-M2 | 中 | `s10 status` 与 gate 不同源（无 baseline 交叉、blocked 提前返回跳过审计） | `internal/cli/s10_command.go:337, 320` | 同一产物两处裁决相反 |
| S10-L1 | 低 | `RecordEvidence --review-round` 显式值不校验与当前轮一致 | `internal/runtime/evidence.go:139` | 索引轮与文件体轮可分叉 |
| S10-WOK | - | Markdown 投影无消费者、8 AuditArea 必达 | `internal/acceptance/markdown.go:21, manifest.go:392` | WIRING_OK：gate 只认 JSON，Markdown 装饰性；8 域 + release_audit clean_round 门真实 |

### 3.6 复杂度↔收益（RC-10/11/12）

| 机制 | 复杂度 | 收益 | 象限 | 判定 |
|---|---|---|---|---|
| RC-10a `ComputeTriple` | 2 | 1.5 | 低/低 | **cut-or-complete**（双层并存、单消费者 housekeeping、零测试） |
| RC-10b journal 轮转 | 4 | 4 | 高/高 | keep（阈值 10k 合理，marker 事务必要；段合并 O(n) 待观察；注释过时） |
| RC-10c `s7RoundRetention=20` | 2 | 2.5 | 中/中 | keep-simple（管增长不管数量，15 families 仍有死指标） |
| RC-10d 17 步 CAS 拆段 | — | — | — | **未执行**，交付为相内计时探针，锁持有分位无从考核 |
| RC-11a agent-message 分层 12+19 | 2 | 5 | 高/低 | keep（但 `WarnMissingExtensionFields` 零生产调用，109 行死角） |
| RC-11b `PLANNER-REFINE` seed | 1 | 3.5 | 高/低 | keep（但 `rejectPlannerPlaceholders` 不认该标记，注册门未闭环） |
| RC-11c `RenderMarkdown` 单源 | 2 | 4 | 高/低 | keep |
| RC-12a `--explain` | 1 | 4 | 高/低 | keep |
| RC-12b `--emit-template` | 2 | 4 | 高/低 | keep |
| RC-12c N/A checklist | 1 | 3 | 高/低 | keep |

### 3.7 Fluency（操作流畅）

| # | 级别 | 标题 | 位置 | 摘要 |
|---|---|---|---|---|
| F-H1 | 高 | S8 ingest 无 batch 时循环 next 指向自身 | `internal/investigation/intake.go:232, 24` | 典型首次拒绝即死循环，未指向 S7 收口 |
| F-H2 | 高 | S10 envelope 无脚手架无示例，必填集只能反推 | `internal/cli/s10_command.go:92, internal/acceptance/manifest.go:606` | manifest 仅有示例，envelope 零示例，错误信息只点名 2 字段 |
| F-M1 | 中 | L3-S9 引用不存在动词 `impact reconcile / handoff submit` | `blueprint/L3-S9-repair.md:618` | CLI 仅 `impact create|commit / handoff create|commit`，权威文档自相矛盾 |
| F-M2 | 中 | 未初始化时 s7/investigation/repair status 裸锁错误 | `internal/runtime/store.go:3133, internal/cli/s7_status.go:95` | 仅 s10 status 给恢复指引 |
| F-M3 | 中 | L3-S7 示例 flag `--assignment` 实际为 `--assignment-id` | `blueprint/L3-S7-verification-round.md:742` | 教错误恢复的示例自身不可复制 |
| F-L1 | 低 | S8 lifecycle 图 ghost `contract_review` | `blueprint/L3-S8-finding-investigation.md:561` | 已按 RC-06 删除，无路径进入 |
| F-L2 | 低 | `--id` vs `--hypothesis-id` / `--assignment` vs `--assignment-id` 不一致 | `internal/cli/investigation_command.go:364` | 无编辑距离提示 |
| F-L3 | 低 | `s7 status` 无 plan 时 next 跳过 scaffold | `internal/cli/s7_status.go:142` | 应指向 `s7 draft` |
| F-L4 | 低 | `--explain / --emit-template` 未入 manual 正文 | `internal/transition/manual.go:76` | 压缩恢复后不可发现 |
| F-L5 | 低 | `runtime evidence add` 唯一强制显式 `--expected-revision` | `internal/cli/run.go:2090` | 与 auto-resolve 语义相反 |
| F-L6 | 低 | next 写 stderr，管道只采 stdout 时丢失 | `internal/cli/repair_command.go:333` | receipt 缺 next_action |
| F-L7 | 低 | TR-008/009 依赖下一次 PreToolUse（blind wait 风险） | `internal/cli/s7_status.go:287` | 有轮询观测，非 blind，但极端场景挂起 |

## 4. 错误路径矩阵（节选）

| 错误注入 | 预期兜底 | 实测 | 位置 |
|---|---|---|---|
| stale `expectedRevision` 并发提交 | 预检 + CAS `ErrStaleRevision` + 清理 + next | RESILIENT | `submit.go:133, store.go:1277, run.go:62` |
| 冻结基线/plan/regression 漂移 | sha 校验 + 标 stale + fail-closed | RESILIENT | `plan.go:78, workspace.go:163, 290` |
| evidence 类型/生产者独立性缺失 | validate + blocked guard | RESILIENT | `submit.go:587, 993` |
| 空/未声明 guard | `catalog.go:527` LoadCatalog fail-closed | RESILIENT | `transition/catalog.go:527` |
| 无 batch 时 ingest | 循环 next（FRICTION） | LEAKY | `intake.go:232` |
| 幽灵 evidence 走通 S8→S9 | 结构门放行 | LEAKY | `case_workflow.go:136` |
| journal 轮转中崩溃 | marker 丢弃致永久非连续 | LEAKY | `store.go:2589` |
| 自引 S10 信封 | gate 放行 | THEATER | `evaluator.go:555` |

## 5. Top 5 最小修复（按 ROI 排序）

1. **[S9-H5] 契约批准接入 `human_boundary` + `approval_hash` 重算校验** — `contract.go:158` 重算 `sha256(draft)` 比对 + `approved_by` 校验 `human_decision` scope 证据（复用 `store.go:2442 / engine.go:496` 模式），`Actor` 改 `human`；否则任意 CLI 持有者可自签 `approved`。
2. **[S9-H6/H8] handoff 走 `transition.Apply` 或显式预算门 + 为 Case/Contract 主链补重试预算** — `runtime.go:1140` 直写 `TR-012` 改走引擎或前置 `configuredMaxFullReviewRounds` 检查；`case_workflow.go:323` 为 `investigate_more↔s9_repair` 补 `max_attempts` 并与 `RepairLimitError→GTR-004` 打通（同时修 H7 桥接）。
3. **[S10-H2] Gate 改调 `ValidateForOutcomeWithBaseline(..., envelope.Conclusion, baseline)`** — 一行调用点错误致 blocked 死锁，函数已写好零调用，`evaluator.go:437` 改调即闭合；同时修 `s10 status` 同源问题（M2）。
4. **[R-H1] journal 轮转恢复按 `pending.ArchivedCount` 截断而非重验 combined 连续性** — `recoverPendingJournalRotationLocked:2548` 在 `inspectJournal` 失败时保留 marker fail-closed 或直接按 archivedCount 截断，避免静默丢弃。
5. **[S8-H1 + S9-H1] hypothesis/result 与 targeted `pass` 断言接 `validateEvidenceReferences` 式 SHA/存在性校验 + 堵 `S9-M5` 未认领兄弟穿透** — 把 reassessment 侧已有的校验下放到主链；S9 `pass` 要求至少一条 evidence_refs；`runtime.go:737` 对 `owner==""` 的兄弟 Assignment 拒绝。

## 6. 可降级 / 可删除候选

| 候选 | 理由 | 建议 |
|---|---|---|
| `ComputeTriple / ComputeRevisionPair` 双层指纹 | 8 家族未删、单 housekeeping 消费者、零测试，净复杂度增加 | 要么迁移 2 个以上真实消费者并补测试，要么删除 |
| `WarnMissingExtensionFields` 19 字段 warn 层 | 零生产调用 | 接线或删除（109 行） |
| S7 指标中 8 个未消费 family | `metrics.go` 15 families 仅 7 被消费 | 裁至 ≤10 可行动指标（C-15 后半） |
| `loop-events.jsonl.archive.*` 段数无界 | `inspectJournal` 每 CAS 读全量段 | 评估阈值或增量索引，非立即删 |
| `s10 manifest render` 外多余 Markdown 模板 | 已单源 | 确认无第二权威后归档旧模板 |

## 7. 对抗式复核（3 视角抽检 10 项高/中）

- 正确性视角：R-H1、S10-H3、S8-H1 复现路径均可构造，重放成立，保留。
- 安全性视角：S10-H1 自证、S9-M3 手动 TR-012 旁路均需“诚实代理”假设，保留；但利用需配合社会工程，非远程可利用，维持中/高而非 critical。
- 可复现视角：所有高均可在单机沙盒用 `go test` + 手工 JSON 重放；4 项降级：S7 site-lost 无指标（观测缺口非控制流）、R-L5/L6/L7（设计取舍）、Fluency L1 ghost 状态（已文档化删除）。

## 8. 补遗（迟到探针 a3d3683 合并说明）

迟到 S9 探针在主报告落盘后完成，补 7 条（见 §3.4 S9-H5/H6/H7/H8/M5/T1/T2），证据 JSON `docs/audits/.evidence/s7-s10-2026-08-28.json` 已合并（`supplement` 字段标记）。结论变化：S9 的“权威批准 + 有限循环”两项承诺当前主要由文档而非运行时承担，优先级应提至 Top 2。

## 9. 证据索引

- 探针产物：`/tmp/audit_harvest_all/*.md`（16 探针）+ `docs/audits/.evidence/*`（上一轮 10 份）
- 关键代码：`internal/runtime/store.go:2657, 1800, 3111` · `fingerprint.go:50` · `internal/qualitygate/evaluator.go:379, 437, 548` · `internal/investigation/case_workflow.go:136, intak
e.go:88, 232` · `internal/repair/evidence.go:151, runtime.go:726, 1179` · `internal/acceptance/manifest.go:187, 392` · `blueprint/L3-*` · `docs/loop-definition.json:1180, 1321, 2137`
- 校验：`go vet ./...` 0 · `go test -count=1 ./internal/...` 33 包绿（`internal/assignment` 11.5s / `investigation` 13.6s / `review` 30.6s / `cli` 27.7s）

---
*本报告为只读审计，未修改任何受审代码；所有 file:line 均经实读核对。*
