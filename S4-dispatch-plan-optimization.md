# S4 整体派发计划与并行波次优化方案

> 日期：2026-09-19
> 状态：设计已应用到 blueprint，waves-v1 首版已实施；进度与验证见 [落地清单](S4-dispatch-plan-implementation-todo.md)。
> 推进顺序：本报告确认设计 → 更新 blueprint → 更新规范、模板和实现 → 验证。
> 目标：S4 交付一份一眼能看懂的派发清单，S5 审查并行与先后是否合理，S6 持续消费同一计划，避免漏派、假串行和提前开工。

## 1. 阅读入口

| 问题 | 入口 |
|:---|:---|
| 当前到底缺什么 | [现状与根因](#current) |
| 新交付物长什么样 | [整体派发计划](#artifact)、[示例](#example) |
| 怎么决定并行、先后和冲突 | [编排规则](#scheduling) |
| S4、S5、S6 如何形成闭环 | [阶段职责](#stages)、[运行时消费](#runtime) |
| 如何避免重复权威和过度设计 | [事实归属](#authority)、[复杂度与收益](#cost) |
| 如何迁移并证明有效 | [实施顺序](#rollout)、[验收](#acceptance) |

<a id="current"></a>
## 2. 现状与根因

用户提出的不是再画一张 DAG，而是把任务集合转化为可以直接照着执行的整体计划：边界清晰、没有真实依赖或冲突的任务并行，有前置产物要求的任务分波推进。计划既要让审查者看出哪里该并行，也要让主会话知道现在该派谁、等什么、何时拿回产出。

本次核对的仓库事实如下；蓝图目标和已经实现的能力分开列示。

| 位置 | 已有内容 | 实际缺口 |
|:---|:---|:---|
| [S4 蓝图](blueprint/L3-S4-task-split.md) | TASK §8 DAG、批次节奏、任务看板 | 批次总览仍是人工视图，没有作为正式派发计划进入阶段出口 |
| [看板模板](docs/tasks/index-template.md) | 里程碑、逐任务矩阵、文本关键路径 | 未要求显式并行组、放行条件和串行理由；负责人、状态等还混入执行态 |
| [TASK 模板](docs/tasks/TASK-template.md) | 范围、依赖、Closing Contract | 需拼接所有 TASK 才能看到全局；依赖状态和生命周期字段存在冻结后回填问题 |
| [tasks check](internal/semantic/tasks.go) | 任务完整性、条款覆盖、依赖引用、无环 | 没有派发计划对账，也没有任务间写域兼容检查；扫描范围仍需收敛到当前需求批次 |
| [S5 蓝图](blueprint/L3-S5-document-verification.md) | TASK 可执行性审查包含“半问：批次节奏” | 没有必须审签的整体编排交付物，难以系统检查不必要的串行 |
| [S6 蓝图](blueprint/L3-S6-build.md) | 目标态已有 Ready Set、即时冲突、集成后释放依赖 | 不能把目标态当作完整落地；其中默认 WIP=2 也需要调整，避免独立任务被惯性排队 |
| [s6 status](internal/cli/s6_status.go) | 展示精确任务批次的 Result、checks、scope deviations、verified checkpoint | 当前命令仅有 status，尚无完整依赖与并行派发计算；不能直接回答下一批该派谁 |
| [team validator](internal/team/validator.go) | manifest 内 assignment 依赖合法性及环检查 | 只覆盖单 manifest，不能替代跨 workgroup 的 TASK 级依赖 |
| [TR-002/003 注册动作](internal/transition/actions.go)、[质量门](internal/qualitygate/evaluator.go) | TASK 登记、S5 精确 subjects、S6 精确集合完成检查 | 缺少同等受审签的整体派发计划及其消费链 |

根因是三个断点：**局部任务描述没有形成正式整体交付物；审查对象没有包含具体编排决策；执行端没有持续计算和呈现下一步。** 仅增加一份无人消费的 Markdown，不能补上这些断点。

<a id="authority"></a>
## 3. 事实归属：一个计划入口，复用现有控制面

采用“静态派发计划 + 实时执行投影”。两者服务不同时间，不各自维护一套完成事实。

| 事实 | 唯一来源 | 其他位置如何使用 |
|:---|:---|:---|
| 当前需求、开发主分支、发布上游 | 已有 REQ 绑定与 runtime | 计划链接到绑定来源；不默认 develop，不另建分支配置 |
| 任务目标、交付物、写范围、语义依赖、验收 | TASK | 计划引用，机器提取；不手写第二份 dependency 表 |
| 当前计划的任务集合、推荐波次、编排理由、必要的资源串行次序 | 每个 REQ 的整体派发计划 | S4 交付，S5 审签，S6 消费 |
| 合同、协议、模型事实 | S3 既有来源 | 通过 TASK 阅读清单渐进展开，不复制进计划 |
| 当前执行批次和批准的计划指纹 | TR-003 注册的当前 generation | 计划中的 TASK 集合必须与其一致 |
| assignment、实际 owner、Result、集成记录 | 现有 runtime、manifest、checkpoint | 看板从这些事实推导，不手工打勾宣告完成 |
| Ready、Waiting、Conflict、Queued、执行进度 | 动态投影 | 可丢弃重建；不得成为新的控制面 |

TASK 依赖描述“需要另一个任务的产物”。资源串行描述“双方不能同时使用同一写入或外部资源”，不能为了排队而伪装成产物依赖。计划中的有向资源顺序只在确有必要时使用，并与 TASK 依赖联合查环，不另建持久化冲突图。

<a id="artifact"></a>
## 4. 正式交付物：把现有 index 升级为整体派发计划

推荐沿用 `docs/tasks/index-REQ-<id>.md`，标题改为“REQ-<id> 整体派发计划”。**不再同时新增另一份 DISPATCH 文件和另一份人工看板。** 已有 REQ 可保留原 index 路径并登记为规范入口，避免纯改名迁移。

文档分四部分：

1. **入口**：REQ、CONTRACTS、适用范围、计划修订号、状态及阅读说明。运行身份和指纹由系统登记，不要求手抄 SHA。
2. **波次清单**：每项 TASK 恰好出现一次，链接到任务书；同波默认可并行；一句话说明这一波的边界及放行依据。
3. **必要的编排解释**：只记录串行原因、共享写入/资源处置和必要集成检查；没有特殊情况就写“无”。
4. **执行入口**：引导 S6 查看 runtime 投影及下一批候选，不在冻结计划中回填 Agent、完成时间或证据。

不要求人重复填写依赖、scope、合同条款、预估工时、字段模型和完整关键路径。机器可在预览中展开 TASK 派生信息；人工只维护有判断价值的编排选择。

### 4.1 todo list 与冻结的关系

S4 计划用 `- [ ]` 展示工作清单，明确这些框表示“待执行计划项”。S5 冻结后它们保持不变；**进度勾选仅出现在 S6 动态投影**，以 verified 集成为完成依据。投影沿用相同波次、TASK 链接和文案，使阅读体验连续。

因此用户看到的是同一种简单清单的计划版和实时版；运行时不通过修改已冻结文件制造新的 SHA 漂移。首版默认命令输出即可，不强制落第二份报告。未来需要导出 Markdown 时，标明生成时间、runtime revision 和“非权威快照”，不能反向写入状态。

### 4.2 最小结构约束

计划元数据声明编排格式版本和所属 REQ；每个波次有稳定局部编号；每个清单项仅绑定一个 TASK 文件链接。编号只在本计划内有意义，无需全球 Wave 注册表。

TASK 的依赖和 scope 仍从当前审核文件视图读取。渲染器可显示这些摘要，但如果输出到正式计划，必须标明为派生区并自动更新，禁止人机双向编辑。首版优先不落派生表，降低同步成本。

<a id="example"></a>
## 5. 示例：先看清计划，再按真实就绪持续补位

以下为格式示意，TASK 路径是示例，不代表当前仓库已有这些任务。它展示“一个共享实现前置 + 两端独立工作 + 最后联调”。

```markdown
# REQ-042 整体派发计划

> REQ: REQ-042
> Dispatch policy: waves-v1
> Revision: 1
> Status: complete

入口：[需求](../requirements/REQ-042.md) · [合同](../contracts/CONTRACTS-042.md)
执行：读取 S6 实时清单；本文不回填进度。

## W1 · 无实现前置，三项并行

- [ ] [TASK-042-01：共享校验库](TASK-042-01.md)
- [ ] [TASK-042-02：前端页面骨架](TASK-042-02.md)
- [ ] [TASK-042-03：后端存储适配](TASK-042-03.md)

并行依据：分别写 packages/validation、web/pages、server/storage。
共享模型已在 S3 提交，三项均只读；无共享可变测试资源。

## W2 · 对应前置回收到开发主分支并验证后启动

- [ ] [TASK-042-04：前端交互接入](TASK-042-04.md)
- [ ] [TASK-042-05：后端接口接入](TASK-042-05.md)

前置产物见各 TASK 的 Dependencies；不要求等整个 W1 结束。
两项分别写 web/features 与 server/api，可以并行。

## W3 · 两端产出回收后联调

- [ ] [TASK-042-06：订单闭环集成测试](TASK-042-06.md)

跨端验证依 SYNC；写 tests/integration，不复制双方模型。

## 编排例外

无。公共 lockfile 由 TASK-042-01 负责，本例其他任务无新增依赖。

## 集成检查

复用 TASK / manifest 中的 required checks；每次集成运行受影响检查。
联调任务消费双方已集成结果，S6 结束仍须通过精确批次检查。
```

本例 TASK 的权威依赖为：04 依赖 01、02；05 依赖 01、03；06 依赖 04、05。计划无需再次手写这些边。

假设 01、02 已经集成，而 03 较慢，04 应立即启动；05 继续等 03。这不是违背计划，而是按计划中的真实依赖推进。波次提供直观布局，不能人为增加“整波齐步走”的屏障。

S6 的实时投影应近似如下：

```text
W1  [x] 01 已集成  [x] 02 已集成  [ ] 03 执行中
W2  [ ] 04 可派发  [ ] 05 等待 03 集成
W3  [ ] 06 等待 04、05 集成
下一批：04；可用槽位：2；其余等待原因如上。
```

<a id="scheduling"></a>
## 6. 编排规则：积极并行，但区分真实约束

### 6.1 三类关系必须区分

| 关系 | 处理 | 示例 |
|:---|:---|:---|
| 真实产物依赖 | TASK 声明依赖，产物回根目录开发分支并验证后放行 | 共享运行库实现 → 导入该库的消费者 |
| 写入或共享资源互斥 | 先缩小边界、拆归属或隔离资源；无法消除才安排串行 | 同一 lockfile、同一测试数据库、同一生成输出目录 |
| 仅共享设计输入 | 允许并行，不增加实现依赖 | FE/BE 读取同一 JSON Schema，分别生成到独立目录 |

前后端角色不同既不证明必可并行，也不意味着必须等后端完成。应检查真实输出与副作用，而不是按角色或层次机械串行。

### 6.2 冲突判断的边界

- 同一父目录不等于冲突。先看具体文件/子目录所有权，目录写域过宽时应收窄。
- 不同文件不等于无冲突。迁移编号、生成器副作用、共享服务状态、公共导出、依赖清单等也可能互相影响。
- 共同只读模型、合同、需求不计写冲突。
- 不用“各自有 worktree”证明无冲突；worktree 隔离工作区，不能消除合并冲突和语义冲突。
- 同一文件不同区块首版不自动判安全。优先拆出公共修改的唯一 owner 或调整模块边界；需要维持同文件并行时，S5 必须明确可执行的隔离/汇总办法。自由文本“不会冲突”不能绕过已有 scope 控制。
- 确实串行时写出具体资源和原因。只有产品依赖写进 TASK DAG；临时资源次序写计划例外，避免污染业务依赖。

机器发现宽路径潜在重叠时给诊断，不把所有目录同名变成硬门。明确违反已批准写域或已有独占约束仍执行现有控制，不以提效为由取消。

### 6.3 波次构造与优先级

从当前计划的非取消 TASK 集合开始，提取真实依赖并查环；按最早可执行层生成初始波次，再根据不可消除的资源约束拆分。同波应能同时开工，不允许隐藏依赖或已知未解决互斥。

若存在资源顺序，与 TASK DAG 联合验证可满足性。把任务排进较晚波次应能说明原因；没有依赖、资源或风险理由的延后列为 S5 优化项。机器提供确定性拓扑分层和诊断即可，不求全局最优排程，不声称波次最长链就是准确工期。

候选超过实际容量时，优先解锁下游多、处于关键依赖链或需要早验证的任务；并列用稳定 TASK ID 排序。无工期数据时只称“关键依赖链”，不伪算精确关键路径时长。

### 6.4 物理容量不能改写逻辑依赖

逻辑并行集合、当次派发批次、运行中的 workgroup 是不同概念。三个无冲突任务只有两个槽时，派两个、一个排队；它仍属于同一可并行波次，不因此产生一条假依赖。

拟调整 S6 固定 WIP=2 的默认设计：使用可用平台槽位、已声明资源限制和可观察的集成积压确定当前容量。不得为了保守而无理由固定两项；也不能超出真实平台能力盲目派发。容量不足只排队，不删任务。

首版不做自适应预测器。主会话/已有配置提供容量，工具显示 active、queued、待集成数和原因；降低并行度需指向具体约束，持续空闲且有候选时给提醒。

<a id="stages"></a>
## 7. S4 → S5 → S6 的完整责任链

### 7.1 S4：交付 TASK 集合和整体派发计划

1. 从已提交的 S3 基线拆分任务，给每项定义真实读写边界与完成判据。
2. 声明实际产物依赖；明确公共文件和生成输出归属，检查共享测试资源。
3. 生成推荐波次，规划者审阅并补最少的编排理由。
4. 对账计划与当前 REQ 的 TASK 集合，检查依赖、资源顺序与波次一致性。
5. 将 TASK 和计划提交到绑定的开发主分支；TR-002 使用上游约定的 Git tree 文件视图登记两者。

不能通过遗漏 TASK 让计划自证完整。成员闭包必须同时对照当前 REQ 的 TASK 声明和合同覆盖宇宙；支持任务虽然不覆盖条款，也必须进入计划。取消项不进执行清单，其覆盖缺口仍由已有检查暴露。存在归属不明的 legacy TASK 必须先诊断，不默认归入或默默忽略。

### 7.2 S5：把“半问”升级为显式编排审查

仍复用两类现有审查职责，不增加第三个 Reviewer 或第三份 PASS。

| 现有职责 | 增加的审查内容 |
|:---|:---|
| 规格一致性 | 计划覆盖同一 REQ/合同/TASK 集合；共享设计输入没有被误解为实现串行依赖；编排没有漏掉契约交付 |
| TASK 可执行性 | 同波能否真实并行；是否遗漏前置/共享资源；是否存在假依赖；公共写入归属是否清晰；前置集成后能否及时启动下游 |

审查输出对整体计划明确给出判断，finding 链接到具体波次、TASK 或资源例外。无需为了每个任务重复写一份审查表；通过时在现有证据中记录覆盖结论即可。

计划必须加入 S5 exact subjects，TR-003 将已审签的计划指纹与精确 TASK 集合一起锁定。S5 之后改波次、资源顺序、成员或编排语义，不能靠 evidence 自动刷新 SHA 掩盖变更，必须回规划/审查流程。首版复用已有返回路径，不新建“仅改计划快速批准”状态机。

### 7.3 S6：按清单执行，按事实放行

主会话入口顺序：**整体计划 → 实时清单 → 下一批 TASK → 各 TASK 的精确阅读清单**。Builder 入口仍是自己的 TASK，只需按需查看所在波次及前置，不强迫读全批次。

每次启动、恢复、有效 Result 到达、集成完成、阻塞变化或槽位释放时重新求值：

1. 从 TR-003 精确集合和批准计划读取任务，不重新扫描全仓猜批次。
2. 按当前有效前置集成事实计算依赖是否满足。
3. 按 active/reported 尚未消费的 assignment 和共享资源计算冲突。
4. 排除已经集成、已有有效 owner 或当前 blocked 的任务。
5. 在可用容量内给出下一批互相兼容的候选；主会话按现有平台派发动作执行。
6. worker 完成后提交子分支；主会话用已有回收流程合并到绑定开发分支、验证、记录，再及时清理。
7. 立即释放依赖和补位；不等待无关慢任务，不把清理提醒变成额外完成门。

动态清单逐项显示 `waiting / ready / queued / running / reported / integrated / blocked` 及简短原因；这些是投影词汇，不要求新增七种 TASK 持久化状态。勾选只表示当前有效集成已满足，Result 上报或 Agent 停止都不能单独打勾。

<a id="runtime"></a>
## 8. 运行时与 Hook：补消费链，不另造调度系统

复用既有 `tasks check`、`s6 status`、assignment、Result、integration checkpoint、S5 subjects 和 TR-006 精确集合门。

首版建议把依赖解析、计划对账和候选求值做成共享内部能力，让 S4 检查、S6 展示与派发入口消费同一结果，避免三处各写一套 DAG 语义。`s6 prepare` 若后续确有创建对象的必要才增加；只读派发建议可先扩展现有 `s6 status`，不为了名字新增空壳命令。

计划和 TASK 是正式设计输入，遵循上游的 `git_tree` 读取规则；执行证据和状态遵循已有明确的磁盘/共享控制面规则。mutable evidence 自动更新绑定 SHA 不适用于计划、TASK 或冻结模型。

Hook 在已有合适事件上向 Agent 提醒：还有哪些可并行任务、谁等待前置、哪些结果待集成、哪些 worktree 待清理。用已核验的平台 Agent 可见通道，不能只输出终端 systemMessage。提醒压缩为下一步和入口，不重复塞入完整计划。

“有 Ready 却未派发”“并行度可能偏低”“worktree 尚待清理”是建议，不增设 Stop/Tool 硬拦截。计划集合不一致、循环依赖、冻结内容漂移等结构性错误进入既有阶段检查；真正的未满足前置和互斥使候选保持等待，并说明原因。入口的 owner/版本校验继续复用已有事务机制，不能把一个可能过期的看板截图当作派发授权。

本提案不依赖新增 Claude Code 专有能力。落地涉及平台事件或工具调用时，应按当时最新官方机制核验；本次没有把旧蓝图中的固定版本号当作当前平台保证。

## 9. 边界情况与恢复

| 情况 | 处理 |
|:---|:---|
| W1 中无关任务很慢 | 已满足真实前置的 W2 任务可启动；波次不是整波屏障 |
| 同波五项只有两个槽 | 两项运行，其余 queued，持续补位；保留完整集合 |
| 前置仅在 worker 分支提交 | 下游仍等待集成；新 worktree 不依赖主会话未提交文件 |
| S4 计划或 TASK 仅在磁盘未提交 | 正式阶段检查不采纳为新版本；提示提交正确开发分支 |
| 公共模型只读，两端独立生成 | 可并行；只有共享生成结果确由另一任务生产时才建立前置 |
| 两项都需要修改 lockfile | 明确唯一 owner、拆公共任务或安排真实串行；不能只靠 worktree 隔离 |
| 一个任务失败 | 阻塞其依赖后代，其他无关任务继续；保留现场并走已有修复路径 |
| Result 已上报但尚未集成 | 显示 reported / 待集成，不能勾选或释放其依赖 |
| 集成检查失败 | 继续等待，不以存在 merge commit 代替 verified；无关任务按兼容性继续 |
| 有历史 verified，后来撤销或被新尝试替代 | 按当前有效 assignment/结果/检查点判定，不把历史 task_id 命中当永远完成 |
| 主会话重启 | 从批准计划和现有 runtime 重建投影，不读人工 checkbox 恢复状态 |
| 计划容量发生变化 | 重新选当次派发批次，不改正式计划，也不要求重新 S5 |
| 修改依赖、写域、资源顺序或任务集合 | 走已有规划返工与重新审签，失效旧版本，禁止静默改冻结内容 |
| 不同 REQ 同时存在 TASK | 仅消费当前绑定 REQ/计划/generation；关联不清先报诊断 |
| 资源排序与 TASK 依赖形成隐含环 | 联合检查发现不可执行顺序，回 S4 调整，不等 S6 死锁 |
| 前置接收后相关产物又发生语义修改 | 复核相关消费者和必要检查；不要求整个仓库 HEAD 永远相同 |

<a id="rollout"></a>
## 10. 落地顺序与影响面

### 第一步：更新正式设计

- [L2](blueprint/L2-lifecycle-plan.md) 与 [S4](blueprint/L3-S4-task-split.md)：正式输出增加整体派发计划，规定当前 REQ 成员闭包和提交视图。
- [S5](blueprint/L3-S5-document-verification.md)：显式审查编排，把计划加入 subjects。
- [S6](blueprint/L3-S6-build.md)：计划波次、实时 Ready、物理容量分离；取消无依据固定 WIP=2；说明提前释放、持续补位、根目录集成后完成。
- [L4 派发治理](blueprint/L4-agent-dispatch-governance.md)：补计划权威、执行投影和派发消费链。已有 L4 足够容纳本机制，无需仅为“波次”新增一篇 L4。
- [L4 运行时](blueprint/L4-runtime-control-plane.md)、[转换机制](blueprint/L4-state-transition-core.md)：补计划注册/冻结/恢复及引用约束。
- 与 [L4 共享模型](blueprint/L4-shared-model-contract-governance.md)、[L4 worktree](blueprint/L4-worktree-governance.md) 保持一致。

### 第二步：更新引导交付物

升级 index 模板为计划；TASK 只保留静态依赖条件，动态依赖状态/owner/生命周期证据改为执行入口引用。同步 [规划 skill](skills/specification-planning/SKILL.md)、[派发 skill](skills/agent-dispatch/SKILL.md)、[Agent 协议](docs/agent-protocol.md)，通过 Markdown 相对文件链接形成“计划 → TASK → 合同/SYNC/模型 → TASK → 执行投影”的闭环。

这一步必须配完整的小型示例，包含独立 FE/BE、一个真实共享实现前置、联调和资源冲突反例。不得将当前代码尚不支持的命令写成可用操作。

### 第三步：实现最小闭环

1. 扩展 TASK 解析与计划解析，先解决当前 REQ 集合选择，避免现有全目录扫描误收其他需求。
2. 增加计划成员精确对账、链接与版本检查、波次/依赖一致性及必要资源顺序查环；机器对潜在 overlap 提示，语义由 S5 判断。
3. TR-002 登记计划，S5 subjects 纳入计划，TR-003 冻结计划和同一 TASK 集合。计划单独区分文档类型，不能冒充 task 或 architecture；实施时同步 schema/注册/筛选，避免改变 S6 的任务分母。
4. 扩展 S6 status 为可重建的 todo/就绪/下一批投影，复用有效 Result 与 checkpoint 的已有判定；与质量门共用事实解码，避免展示已完成而门判未完成。
5. 现有派发入口消费同一候选求值，并复核 runtime revision、owner 与资源条件；计划提供建议不等于平台 Agent 已成功启动。
6. 在已有生命周期提示上增加简洁的下一步；不新增后台调度服务或轮询守护进程。

必须把第 3～5 项连起来验证后，才称“派发计划落地”。单有新模板或更漂亮的 status 只能算引导层改进。

### 第四步：迁移与验证

历史已经执行的 generation 不强制补伪造计划或追认 S5。对 legacy 显式展示“缺少受审签的整体计划”，保留恢复现有任务的能力；新规划批次采用新格式，返工到 S4 时补齐。兼容分支不得让新批次借 legacy 标记绕过新交付要求。

先跑一个多波且存在提前释放的小型系统案例，再到具体 REQ 试用。根目录报告保留设计理由和实施状态，blueprint 成为正式规范权威。

<a id="acceptance"></a>
## 11. 验收矩阵

| 场景 | 必须观察到的结果 |
|:---|:---|
| 三个无依赖且边界不冲突任务 | S4 同波；容量足够时下一批同时包含三项 |
| A→B，C 独立且很慢 | A 集成后 B 就绪，不等待 C |
| 两任务依赖同一共享库 | 库集成前都等待；集成后两项并行 |
| 只共享只读 Schema 的 FE/BE | 不产生假的 FE→BE 或 BE→FE 边 |
| 相同公共文件写入 | 发现风险并有明确所有权/隔离/串行处置，不能靠两 worktree 自动放行 |
| 路径不同但共享测试数据库 | 资源约束可见，不把“文件不冲突”当全部安全证明 |
| TASK 缺失、重复、取消后残留或跨 REQ 混入 | 计划与成员检查给出具体对象，不能通过缩小计划藏任务 |
| TASK A 依赖 B，但资源顺序要求 A 先于 B | 联合图准确报告不可满足的环，不等执行时死锁 |
| 已审签后改计划、TASK 或未提交文件替换 | 漂移/提交视图检查生效，证据刷新不能绕过 |
| Worker 返回成功但未回收 | 不勾选、不释放消费者；显示集成下一步 |
| 容量不足与主会话恢复 | 无漏项、无重复 owner；恢复后相同事实产生相同候选 |
| 历史集成成功但当前有效尝试失败/失效 | 看板与阶段门结论一致，不复用失效成功 |
| 新计划文档加入 documents | 不被算作 TASK，不替代 S2 设计，S5 subjects 精确覆盖 |
| 平台派发失败 | 不伪造 running；沿既有状态恢复，任务仍可重试 |
| Agent 收到提示 | 能看到下一批和阻塞原因，非仅终端有文本；提醒不构成新 Stop 门 |

效果看三项：可派发任务是否无故空等、独立任务是否实际并行、集成返工是否增加。再记录批次完成时间与主会话人工重建顺序的次数。首版不预设“效率提高百分之几”，需要真实 REQ 对比验证。

<a id="cost"></a>
## 12. 逻辑自审与复杂度收益

本方案值得做的部分是补全一条已有但断开的消费链：TASK 已有 DAG、S5 已有两路审查、S6 已有批次门和集成记录。新增成本主要是一份升级后的 index 和共享求值逻辑，收益是把全局编排从 Agent 的工作记忆移到受审签、可恢复的交付物。

本次设计主动约束四个容易膨胀的地方：

- 不要求 TASK 和计划双写同一套依赖；计划引用 TASK，特殊资源顺序才新增事实。
- 不让静态计划兼任实时状态；checkbox 的执行结果由现有 runtime 推导。
- 不把波次变成全局屏障；只等真实前置，容量限制只影响当次派发。
- 不为了并行引入预测排程器、全局资源锁平台、新 Agent 角色、独立 Wave 生命周期或强制审批。

仍然需要承认：静态路径分析无法证明语义无冲突，Markdown 计划也不能证明平台真的并行启动。前者由 S5 的具体边界审查与集成检查承担，后者必须用派发记录和实际运行验证。若只完成文档，没有让 S6 消费依赖和返回下一批，收益不足以支持宣称整改完成。

## 13. 本次交付范围

第 1—12 节保留设计推导和当时的现状核对。正式规范已迁入 blueprint；首版实现及实际验证以 [落地清单](S4-dispatch-plan-implementation-todo.md) 为准，不能把提案中全部目标等同于实测完成。
