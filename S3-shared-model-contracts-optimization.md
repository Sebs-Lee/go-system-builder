# S3 共享数据模型、合同推导与渐进式阅读优化设计

> 日期：2026-09-18  
> 自审修订：2026-09-19  
> 状态：已按自审收敛范围完成首版蓝图、规范与实现；验证与适用边界见第 17 节。  
> 工作顺序：根目录优化文档 → blueprint 正式设计 → 规范、模板与 skill → 实现与验证。

本文记录本轮讨论的问题、根因、设计决定和具体落地方案。它是工厂仓库的优化报告，不是目标业务项目的 REQ、正式机制蓝图或已经通过的验收证据。后续应把确定的机制迁入 blueprint，由蓝图成为规范权威；本文保留问题来源、设计理由、实施范围与完成记录，不能长期成为第二套规范。

第 1—16 节保留优化设计与自审依据，包含未来可选能力，不等同于全部已实现。首版实际交付以第 17 节及正式蓝图为准。

## 1. 阅读入口

| 要了解什么 | 阅读位置 |
|:---|:---|
| 为什么需要调整、当前落地缺什么 | [问题与现状](#problem) |
| 哪个文件拥有哪类事实 | [权威与模型边界](#authority) |
| 共享模型如何组织和被实现消费 | [模型包](#model-package)、[实际消费](#consumption) |
| 合同应按什么顺序推导 | [推导顺序](#derivation) |
| Agent 从任务出发如何自然阅读 | [阅读路径](#reading)、[文件链接规范](#navigation) |
| 如何与阶段、Git、worktree 及变更控制衔接 | [阶段接线](#stages)、[基线与变更](#baseline) |
| 如何落地及证明有效 | [实施顺序](#rollout)、[验收矩阵](#acceptance) |
| 自审发现了什么、首版成本是否值得 | [逻辑与收益复审](#self-review) |

<a id="problem"></a>
## 2. 问题、用户意图与当前证据

### 2.1 问题

S3 虽然产出 FE、BE 和 SYNC 合同，但前后端仍可能分别推测字段、状态、错误和接口语义，直到实际联调时才发现偏差。SYNC 约定了验收和对账要求，却没有充分承担开发之前的共同协议设计；检查发生得太晚，返工发生在双方已经实现之后。

用户要求：像 S2 维护模块当前原型一样，S3 应维护项目级、全局唯一的数据模型事实源。前后端基于同一模型开发。同时必须编排合同推导顺序和角色阅读顺序，通过 Markdown 文件链接实现渐进式披露，让 Agent 自然阅读即可理解当前任务、共同依据和完成方式。

### 2.2 已核对的现状

| 位置 | 当前行为 | 缺口 |
|:---|:---|:---|
| [S3 蓝图](blueprint/L3-S3-contracts.md) | FE → BE → SYNC；最终并排对齐字段和行为 | 共同事实与协议晚于分端合同形成 |
| [规划 skill](skills/specification-planning/SKILL.md) | Step 10 延续 FE → BE → SYNC | Agent 的执行顺序直接复制上述问题 |
| [数据模型模板](docs/design/data-model/MODEL-template.md) | ER 图、实体、字段、索引、迁移 | 偏向持久化，缺少共同传输模型及明确消费者 |
| [SYNC 模板](docs/contracts/SYNC-contract-template.md) | 独立填写请求响应、字段、错误和状态关联 | 容易形成另一份权威定义 |
| [FE 模板](docs/contracts/FE-contract-template.md)、[BE 模板](docs/contracts/BE-contract-template.md) | 分别引用需求、场景、对端和联调合同 | 未明确绑定同一模型及协议闭包 |
| [合同检查](internal/semantic/contracts.go) | 检查 token、条款、场景覆盖及部分指纹 | 无法证明字段结构、模型来源及双方消费基线一致 |
| [fixture schema](internal/schema/assets/scenario-fixture-contract.schema.json) | 定义合成前置事实的 setup/cleanup | 未把具体数据绑定到共享结构 |
| [TASK 模板](docs/tasks/TASK-template.md) | 已有 Document Manifest、范围、依赖和 Closing Contract | 尚未形成模型 → 协议 → 分端责任的精确阅读链 |
| [场景规则](docs/rules/scenario-model.md) | 模块当前真相唯一，REQ 不拥有场景副本 | 可复用此治理原则，但场景模型不等于业务数据模型 |

上述证据支持的结论是：现有系统已具备合同、场景和引用检查基础，但缺少共享数据与协议的源头治理和实际消费机制。不能仅通过增加一份 Markdown 或延长 S5 检查清单解决。

## 3. 目标与设计约束

1. 每个事实只有一个权威定义；合同、类型、Mock、测试围绕该定义建立可追踪关系。
2. 先设计共同数据与协议，再拆分两端责任，尽早暴露歧义。
3. 项目维护当前模型体系；REQ 记录变化来源，不拥有独立模型副本。
4. 分别说明推导顺序、事实权威和阅读顺序；首先通过现有模板与链接表达，不因此创建三套机器图模型。
5. Agent 从当前任务入口获取足够执行信息，再按明确链接展开必要依据。
6. 复用已有阶段、Writer、基线登记、文件视图、变更控制和场景分母，不建立另一套审批系统。
7. 自动检查负责可判定的结构与引用；业务设计正确性仍需独立审查和真实验证。
8. 模型是正式设计资产；不能作为 mutable evidence 自动刷新哈希来消解设计变更。

不追求：所有数据塞进一个巨型文件、把数据库实体直接暴露给前端、所有领域强行共享一个 User 类型、用生成测试取代独立验收、给每个链接都新增 Hook 硬拦截。

<a id="authority"></a>
## 4. 事实权威与模型边界

### 4.1 权威按事实归属确定

| 事实 | 唯一权威 | 下游职责 |
|:---|:---|:---|
| 业务目标、范围与验收要求 | REQ | 引用和实现，不改变需求含义 |
| 领域边界、业务规则、状态转换、用户行为 | S2 对应设计与模块场景包 | 翻译为精确数据和协议约束 |
| 字段、类型、单位、约束和传输结构 | 共享数据模型 | 引用、生成或校验，不复制定义 |
| 操作、跨端时序、错误、幂等及恢复协议 | SYNC 对应协议定义 | 分端实现及联合验证 |
| 前端或后端的实现责任 | FE / BE | 明确交付边界和映射责任 |
| 一次执行范围、依赖、交付物和完成证明 | TASK | 在授权范围实现已确定条款 |
| PASS/FAIL、执行日志及观察结果 | 对应轮次 evidence | 回证条款，不创造业务规则 |

这不是“后写的文档覆盖先写的文档”的优先级。下游只能细化上游；冲突必须返回事实所属层修订。TASK 不能授权绕过协议，FE 不能私自改变错误枚举，SYNC 不能改变 REQ 的验收含义。

### 4.2 三层模型及显式映射

| 层次 | 内容 | 主要消费者 |
|:---|:---|:---|
| 领域模型 | 实体身份、关系、业务含义、共享值类型、不变量、状态引用 | 架构、前后端、测试 |
| 传输模型 | 请求、响应、事件、序列化、错误结构、兼容规则 | 客户端、服务端、Mock、契约测试 |
| 存储与展示映射 | 表结构、接口投影、前端 ViewModel 的转换规则 | 持久化实现、适配层、展示层 |

三层是职责划分，不要求三个独立 schema、三份文档或一套通用映射语言。简单同构场景直接引用同一定义；存在实际转换时才记录映射，可由适配代码和测试承担。

同一事实只有一个源，不等于三个层次使用同一个类。数据库可能保存内部审计字段，响应只暴露授权投影，页面可以把金额转为显示文本；这些转换必须有明确映射和测试。

不同领域中的身份、客户、账户可以是不同概念，以领域命名区分。共享身份标识或金额语义才应抽为公共定义。不得仅因名称相同便强行复用，也不得仅因消费者不同便复制相同语义。

### 4.3 字段与操作必须消除的歧义

- 业务含义、类型、范围、精度、单位与编码格式。
- 必填、可空、缺失、空字符串、空集合的区别。
- 默认值由谁产生、在何时产生；Schema 注解不等于运行时自动填充。
- 枚举及未知值的处理；ID、日期、时区和时间精度。
- 只读/可写、权限、敏感字段与暴露边界。
- PATCH 中省略是否保持、null 是否清空；创建与更新是否采用不同投影。
- 关系、状态引用、跨字段约束、并发版本和幂等标识。
- 请求响应、分页、错误结构和支持版本的兼容策略。

示例：金额必须说明是“人民币分的整数”还是“元的十进制字符串”；不能仅写 number。状态转换继续由既有状态机设计拥有，模型引用它，不在多个合同里各写一张权威状态表。

<a id="model-package"></a>
## 5. 项目级共享模型包

### 5.1 建议结构

沿用现有 `docs/design/data-model/`，目标项目可采用以下结构；已有可用契约资产时通过登记复用原文件，不强制搬迁和复制。

```text
docs/design/data-model/
├── MODEL.md             # 入口、领域边界、权威规则及阅读导航
├── registry.json        # 可选：多来源时的最小定位登记，首版不要求
├── domains/             # 领域实体、共享值类型和约束
├── interfaces/          # 请求响应、事件及机器可读接口定义
├── mappings/            # 持久化、传输、展示映射
└── examples/            # 经模型验证的正反样例
```

全局唯一是一个有统一入口和权威规则的模型体系，可以按领域拆文件。上面的目录是可扩展组织示例，不是必须创建的交付清单。首版可以只有 MODEL.md 入口和项目已有的一份机器契约；domains、mappings 和 examples 按实际需要拆分。

优先以原生 Schema ID、协议定义路径和 fragment 定位，不额外发明模型 ID。只有多个异构来源确实无法通过现有 CONTRACTS 索引清楚定位时，才增加 registry；它只拥有定位关系，不能重复保存字段定义。MODEL.md 负责解释和导航，不再人工复制完整 Schema 表。

各定义应可稳定定位，源设计和依赖关系按需通过既有索引或原生引用表达；不要求每个字段增加一组手填元数据。精确提交、指纹及冻结状态由现有 runtime 基线登记持有，TASK 和 registry 不手工维护第二套运行时版本账本。

### 5.2 权威源与派生物

结构必须机器可读；解释、理由和无法直接表达的行为约束可以保留在 Markdown。HTTP/JSON 可采用 OpenAPI 配合其支持的 Schema 方言；已有 Proto 等协议则复用原生契约。具体版本与工具支持在实现阶段核实和固定，不在本文臆定通用生成器。

每类事实选择唯一作者源。例如独立共享 Schema 拥有字段结构，OpenAPI 引用它；也可由既有 OpenAPI components 拥有该结构，其他消费者引用它。禁止两处手写同义结构再依赖人工同步。

SYNC 的机器可读协议与 Markdown 是同一协议包的不同载体：机器文件拥有可机器表达的操作结构；Markdown 拥有交互解释和职责映射；表格或请求样例若从机器源展示，应标为派生并可验证。

源文件、生成器版本与配置、输出位置应可追踪。生成类型、客户端、文档投影和 Mock 不能反向成为新的权威。生成物不得手工修改；例外必须通过显式适配层实现。

### 5.3 与 S2 场景的关系

共享数据模型不替代 `scenario-model.json`。场景包拥有业务规则分支、CASE 和 oracle；数据模型拥有结构与字段约束。两者通过稳定引用关联，避免再次复制规则。

fixture 继续只建立业务动作之前的事实，不执行被测动作。对实际数据样例增加模型引用和结构校验；不把现有 setup/cleanup 的自然语言步骤强行解释为 JSON 数据。

负样例分为两类：结构负例（缺失必填项、类型不符）应被 Schema 拒绝；业务负例（合法请求但权限不足、状态不允许、并发冲突）应先通过 Schema，再按 SYNC/CASE 验证业务拒绝及副作用。必须声明预期拒绝发生在哪一层，不能把所有 negative CASE 当成非法数据。

<a id="derivation"></a>
## 6. 推导顺序与各类合同职责

### 6.1 正向推导依赖

```text
REQ：目标、范围、验收
  → S2：领域、架构、状态和场景
  → S3 共同基线：共享模型 + SYNC 协议协同细化
      领域概念 → 操作与场景 → 请求响应/错误模型 → 行为对账
  → FE / BE：分端职责（可并行）
  → CONTRACTS：范围与覆盖关系收口
  → TASK：具体交付、依赖和完成证明
```

这表示共同基线先于分端合同定稿，不是先完成全项目模型、再开始所有接口的瀑布流程。请求/响应由具体操作塑造，不能先封死 DTO 再让操作适配；以本次受影响的业务切片迭代模型和协议，收敛后再锁定分端责任。既有未受影响领域无需重新设计。

CONTRACTS 索引可以从 S3 开始持续建立；“收口在后”不表示到最后才创建。模型与协议设计也允许迭代，但在分端合同最终确定之前，共同基线必须明确。发现上游缺口应返回修订，不能由下游自行补语义。

用户界面需求仍是模型和协议的重要输入。取消 FE 先定义数据的默认顺序，不等于让后端单方设计接口。

### 6.2 S2 / S3 边界

- S2 决定业务概念、架构边界、允许的状态转换和用户行为。
- S3 将这些决定落实成精确的数据、接口和跨端承诺。
- 业务含义未明确：返回 S2；需求立意未明确：返回 REQ 变更路径。
- S3 不另造模块场景分母，也不把 S2 原型中的临时示例直接当成完整 Schema。

### 6.3 SYNC 的新定位

SYNC 是共享协议的执行合同，与共享模型共同收敛，先于分端责任定稿。它应明确：

1. 操作/事件与模型引用，前置条件及结果。
2. 身份认证、授权、请求关联、幂等和限流。
3. 超时、取消、重试、分页、并发和异步完成。
4. 错误出现时提供方承诺、消费方反馈及恢复。
5. 状态变化、持久化效果与禁止副作用。
6. CT、CASE 及真实联调所需的验证要求。

示例：模型定义取消订单请求和 `ORDER_NOT_CANCELLABLE`；SYNC 规定并发完成时后端返回此错误、前端刷新订单并恢复操作状态，且不得展示取消成功。FE/BE 分别承担这些行为的实现。

FE/BE 不再独立定义数据结构，重点描述本端职责、适配映射、边界处理和完成标准。只有单端或无网络交互时，按实际边界声明适用性，不强造 FE/BE/SYNC 三件套。

<a id="reading"></a>
## 7. 阅读顺序：任务进入、按需展开

### 7.1 三种关系分别表达

| 关系 | 回答的问题 | 规则 |
|:---|:---|:---|
| 事实权威 | 冲突时哪个定义负责 | 按事实归属，不按文件先后 |
| 推导依赖 | 哪些决定必须先完成 | 有向无环，可按领域并行 |
| 角色阅读 | 当前 Agent 先看什么 | 从角色入口渐进展开 |

导航和语义引用可以互相链接；递归模型也是合法结构。要求无环的是 TASK 执行先决条件和明确声明的定稿依赖，不是所有文件引用。TASK 必读清单采用有限、去重的有序列表，不递归展开全部链接；首版不新增全项目阅读图环检测。

### 7.2 Builder 默认路径

```text
TASK：目标、范围、依赖、阅读清单、完成标准
  → 本端合同相关条款
  → SYNC 相关操作与行为
  → 共享模型相关定义
  → 必要的状态机、场景或架构解释
  → 返回 TASK 执行和验证
```

TASK 首屏必须让 Agent 知道交付什么、允许改什么、依赖是否满足、依次读哪里、如何证明完成及遇到缺口去哪里。禁止要求 Agent 先通读整个项目再自行拼装任务。

Document Manifest 按当前任务声明准确顺序，不要求所有角色使用同一清单。字段至少包含顺序、文件/锚点链接、阅读目的、必读/条件阅读以及触发条件。已读的同一基线切片无需反复展开；新上下文通过入口恢复。

### 7.3 角色入口

| 角色 | 入口 | 主要路径 |
|:---|:---|:---|
| Main / Planner | CONTRACTS 索引 | 设计 → 模型 → SYNC → 分端 → TASK |
| Builder | 当前 TASK | 本次条款 → 协议 → 模型 → 所需解释 |
| Reviewer | 审查范围与 subjects | TASK/实现 → 合同 → 共同基线 → REQ；检查遗漏和冲突 |
| 联调验证者 | SYNC 操作/验证入口 | 模型 → 两端职责 → CASE → 实际证据 |
| 修复 Builder | BUG 与修复 TASK | 已定位根因 → 受影响合同/模型 → 修复及回归要求 |

这些是同一权威文档体系的不同入口，不为角色复制专属事实文档。

<a id="navigation"></a>
## 8. Markdown 文件链接与渐进式披露规范

### 8.1 文档导航结构

以下是按角色选用的导航要素，不要求每个小文件增加七个章节；短文档可合并为入口说明、正文及出口。

1. 开头：本文件职责、范围、事实权威和角色入口。
2. 必读依赖：按顺序给出精确文件/章节，解释为何阅读。
3. 正文：仅定义本层拥有的事实，相关条款就近链接上游。
4. 可选背景：与必读项明确分开，避免全部展开。
5. 下游消费者：链接到唯一消费索引或生成的反向索引。
6. 缺口处理：按问题类型给出返回权威层的链接。
7. 完成出口：验证要求、证据登记及下一步。

仓库内使用相对 Markdown 文件链接和稳定锚点，保持 clone/worktree 可移植性；不把本机绝对路径写进模板。对机器定义使用稳定 Schema ID/JSON Pointer；Markdown 导航可先指向解释页，再精确标明机器定义位置。不得假定所有 Markdown 渲染器都能把 JSON fragment 当成章节打开。

标题重命名不应破坏稳定业务锚点。相对路径从链接所在文件解析，锚点按项目约定验证。不得用裸代码路径替代需要点击阅读的文档链接。

### 8.2 TASK 阅读清单示意

下列是目标项目的示意路径，并非本仓库已存在的文件：

```markdown
1. 阅读 [前端合同：订单取消](../contracts/FE-orders.md#cancel-order)，确认本次责任。
2. 阅读 [联调协议：取消订单](../contracts/SYNC-orders.md#cancel-order)，确认错误与恢复行为。
3. 阅读 [取消请求模型](../design/data-model/interfaces/orders.md#cancel-request)，确认数据约束。
4. 阅读 [订单状态转换](../design/state/order.md#cancel)，确认允许和禁止的转换。
```

链接文字说明“读什么、为什么读、读完知道什么”。对同一文档只引用任务相关切片；不得把整套模型包都默认标为必读。若必读闭包仍过大，应重新拆分任务，不能通过漏读依赖解决。

### 8.3 双向可追溯，不双向手工登记

```text
REQ / S2
  → 模型与协议
  → 分端合同
  → TASK
  → 实现及验证证据
  → 回证合同条款与需求验收
```

下游声明消费的上游引用；首版通过既有 CONTRACTS/TASK 索引和按引用查询回看消费者，无须建设通用知识图谱或持久化反向索引。需要扩展时才在解析结果中区分来源、消费、验证和导航；不要求 Agent 手工填写所有关系类型。必读列表拥有显示顺序，事实关系拥有依赖语义，不能互相替代。

CONTRACTS 继续拥有合同条款宇宙；TASK 继续声明交付条款；模型定位优先使用原生定义和现有索引。将来确有必要生成反向索引时从这些来源派生，不再维护第二套手写覆盖矩阵。

S3 不能预填 S4 尚未产生的 TASK 或 S7 尚未产生的证据。现有“派生任务”章节应改为消费索引入口；真实结果在产生后登记，不回写锁定合同。Markdown 链接可导航 evidence，但运行时是否接受该 evidence 仍按其身份、轮次和来源契约判定。

<a id="consumption"></a>
## 9. 代码、Mock 和测试必须实际消费基线

| 产物 | 消费方式 | 核验方式 |
|:---|:---|:---|
| FE 类型/客户端 | 优先同源生成，展示类型显式映射 | 重生成比较、类型检查、消费方测试 |
| BE DTO/边界校验 | 同源生成或适配器验证 | 请求响应结构及错误测试 |
| Mock/正样例 | 引用权威模型 | 对 Schema 验证并使用真实协议约束 |
| 结构负样例 | 声明预期违反的 Schema 约束 | 结构校验在正确位置拒绝 |
| 业务负样例 | 使用合法结构及业务前置条件 | 业务层按协议拒绝，并核验副作用 |
| Provider 测试 | 观察真实服务输出 | 响应、错误、权限和副作用 |
| Consumer 测试 | 检查实际请求和消费逻辑 | 构造、解析、未知值及恢复 |
| 真实联调 | 使用真实双方实现 | 认证、时序、持久化、并发与恢复 |

生成器、参数及依赖版本应固定，保证可重复。生成代码策略由项目选择，但用于阶段交付的正式源文件、实现与测试必须遵循已提交输入规则。

不能把“手写了 model_ref”当成已经同源消费。没有合适生成器的技术栈，首版可通过边界验证器和契约测试兑现；必须明确覆盖和限制。

生成的 Mock 和生成的测试可能共同复现错误模型。S5 必须独立对照 REQ/S2 审查模型；S7 必须保留真实行为与副作用验证。Schema 不能自动证明业务正确、权限正确或所有兼容性条件。

<a id="stages"></a>
## 10. 阶段、检查与运行时接线

| 阶段 | 新增或调整责任 | 应消费的结果 |
|:---|:---|:---|
| S2 | 明确领域语义、状态和场景；给出模型设计输入 | 需求与设计事实，不提前完成两端实现 |
| S3 | 模型与 SYNC 共同收敛 → FE/BE 定稿；校验及提交 | 模型闭包、协议、合同和规划证据 |
| S4 | 按真实产物依赖拆分必要基础任务；编排阅读路径 | TASK 依赖 DAG 及精确文档清单 |
| S5 | 将模型及协议闭包纳入 subjects 和基线冻结 | 独立审查及精确基线 |
| S6 | 依赖的共享实现先集成，其余任务可并行；双方消费共同契约基线 | 实现、生成物、契约测试和回收结果 |
| S7 | 验证真实实现、模型一致性及完整行为 | CT 与既有 QA/E2E/REV 证据 |
| S8/S9 | 定位设计或实现偏差，按所属层修复 | 影响范围、合法变更及定向/完整回归 |
| S10 | 沿已有 AC/CASE/条款回收证据 | 模型和协议承诺得到实际验证 |

### 10.1 可机械检查的范围

- 模型 ID、操作 ID、权威路径和 fragment 存在，重复权威声明被识别。
- 引用闭包完整；远程依赖若存在，应固定内容或版本，不能检查时漂移。
- Schema 合法，正反样例符合声明的验证预期。
- 合同引用存在并属于正确基线；双方共同操作指向同一结构权威。
- 复用 TASK 先决条件的 DAG 检查；阅读清单有序且去重。原生模型引用按协议解析，不能通用拒绝递归结构。
- 当前任务的必读文件链接及稳定章节锚点有效；可选背景坏链先作为诊断，不因全库历史链接阻断本次阶段。
- 生成产物与声明源、工具配置相符。
- 已有 CASE → 合同 → TASK 覆盖和证据回链继续有效。

本节是可实现能力范围，首版取舍见[复审结论](#self-review)。校验优先针对当前合同批次的权威输入闭包，跨批次复用的定义按引用纳入；不在每次 Hook 上无差别扫描全项目全部契约和链接。缓存只可按精确输入快照及检查配置复用，不能以缓存略过输入变化。

第一版不能声称自动识别所有语义重复类型；可以检查相同 ID 的冲突声明，近义模型和不合理领域拆分仍由审查处理。兼容性差异工具提供证据，不能独自替代消费者分析。

### 10.2 集成位置

复用 [合同检查](internal/semantic/contracts.go) 及现有 `contracts_checked` 阶段入口；模型解析可以建立独立内部模块，再被检查器和生成工具共同消费。复用 [文件视图](internal/fileview/view.go)、[文档登记](internal/transition/actions.go) 和既有 runtime 基线体系。

正式阶段检查读取上游输入契约指定的已提交 Git tree；编辑期诊断可以显式读取磁盘，但不能据此推进 stage。一次检查的发现、读取、哈希及登记须使用同一快照。链接校验也遵循相同视图，不能用磁盘上刚创建的文件补齐 Git tree 中缺失的目标。

Hook 负责提醒和触发现有检查，不承担另一套模型审批。worktree 软提醒继续保持软提醒；正式阶段输入资格仍由相应 gate 判定。

<a id="baseline"></a>
## 11. 提交基线、worktree 与变更治理

### 11.1 从设计到并行实现

S3 模型及合同必须提交到 REQ 绑定的开发分支。S5 冻结该批审查基线；S6 的 assignment 通过既有运行时身份引用它，而不是手抄指纹。

模型已提交不代表客户端、适配器或迁移已经实现。确有共享实现产物被其他任务依赖时，才拆为 S6 基础 TASK：先完成、在 worker 提交、合并回权威根目录并验证，再派发依赖其实现的 worker。

若 FE/BE 可以从同一已提交 Schema 各自在不同输出目录生成类型，则可以直接并行，不要求人为增加“共享生成任务”。持久化迁移不天然阻塞前端基于协议的开发。依赖关系必须源于实际需要的产物和写路径，不能因某项被命名为“地基”就让所有任务等待。

权威根目录及显式分支绑定继续遵循 [Worktree 机制](blueprint/L4-worktree-governance.md)。不默认 develop；worker 成果按既定合并提交流程回收。这是开发集成，不是 release。

### 11.2 唯一当前真相与历史快照

基线一致性按共同消费的权威文件及其传递引用闭包判断，不要求 FE/BE 的整个 checkout commit 相等。根目录在接收基础实现后产生新 commit，只要冻结的设计内容未变，新的实现基线仍可使用；模型发生变化则走既有重新审查路径。

项目当前模型体系唯一；任务可以固定读取 Git 历史快照，以确保复现。这不构成另一份手工维护的当前模型。禁止在 REQ 目录复制同一模型来绕过冲突。

模型、协议或映射发生语义变化，应走 [变更控制](docs/rules/change-control.md)，计算传递影响，更新受影响合同、任务、生成物和验证，并按已有状态机重新审查/登记。不能修改一端代码后在 evidence 中解释为“兼容处理”来替代设计变更。

### 11.3 主要边界

| 场景 | 设计要求 |
|:---|:---|
| 仅 UI 样式变化 | 引用现有模型，明确无模型变更，不制造额外模型文件 |
| 后端内部优化 | 核对存储映射影响，不要求无关 FE 修改 |
| 删除字段、收紧约束、增加枚举 | 分析实际消费者、未知值行为和部署顺序；新增枚举不自动等于兼容 |
| 新旧客户端并存 | 在唯一当前体系内声明受支持的协议版本、映射和退役条件 |
| 两个 REQ 同时改模型 | 固定各自快照；回收时做语义与兼容性对账，Git 无冲突不等于无设计冲突 |
| 并行修改共享文件 | 拆明确所有权或串行集成；重新计算影响，不让两端各保留一份定义 |
| S6 发现模型缺口 | 返回所属设计层，经既有变更路径更新共同基线 |
| 外部系统不可控 | 固定外部契约，通过适配层映射内部模型，验证观测到的实际行为 |
| 本地模型未提交 | 允许编辑期诊断，但不能帮助正式阶段通过 |
| evidence 合并后哈希变化 | 按 evidence 专用机制更新；不得连带刷新模型或冻结 subject 来消除差异 |

<a id="rollout"></a>
## 12. 落地顺序与具体改动范围

### 12.1 第一步：本文

记录根因、已达成的设计方向、目标行为、实施依赖和验收要求。本文完成不意味着蓝图和实现完成。

### 12.2 第二步：正式蓝图

建议新增 `blueprint/L4-shared-model-contract-governance.md`，承担权威归属、引用图、派生物、基线和演进机制；实际命名在迁入时统一登记。

同步修改：

- [L2 生命周期](blueprint/L2-lifecycle-plan.md)、[L3 入口](blueprint/L3-README.md)：反映新依赖与交付链。
- [S2](blueprint/L3-S2-design.md)、[S3](blueprint/L3-S3-contracts.md)：领域设计边界、共享模型与协议先行。
- [S4](blueprint/L3-S4-task-split.md)：基础任务依赖及角色阅读清单。
- [S5](blueprint/L3-S5-document-verification.md)：模型与协议闭包进入 subjects。
- [S6](blueprint/L3-S6-build.md)、[S7](blueprint/L3-S7-verification-round.md)：同源消费、实际验证。
- [运行时控制面](blueprint/L4-runtime-control-plane.md)、[状态迁移](blueprint/L4-state-transition-core.md)、[派发治理](blueprint/L4-agent-dispatch-governance.md)：登记、输入视图、assignment 与上下文披露。
- 视影响同步 S8/S9/S10 及蓝图索引，避免链路末端继续消费旧定义。

蓝图写最终规范，本文保留优化理由和进度，不把整改待办作为 L4 机制正文。

### 12.3 第三步：规范、模板与 Agent 指引

修改 MODEL、CONTRACTS、FE、BE、SYNC、TASK 模板；同步 [Agent 协议](docs/agent-protocol.md)、[合同规则](docs/contracts/README.md)、[API 规则](docs/rules/api-design.md)、[场景规则](docs/rules/scenario-model.md) 及相关 skill。

重点移除或改写：FE → BE → SYNC 固定顺序、手写重复字段权威、预填未来派生任务/证据、缺少理由的裸路径阅读清单，以及“合同唯一依据”中忽略被引用上游基线的歧义。

既有英文规范保持其语言约定。新增模型规则、可选定位登记和导航约定时，应同时注明生产者、消费者和校验责任；无实际消费者的字段不进入模板。

### 12.4 第四步：实现

1. 选定试点的一种既有机器契约格式，沿用原生引用；在 MODEL/CONTRACTS 中明确权威入口。
2. 扩展现有合同检查：本批次必要引用、Schema/样例、TASK 必读链接；复用 TASK DAG。
3. 将实际引用的模型与协议文件闭包纳入提交视图、文档登记与 S5 冻结；assignment 引用既有身份。
4. 复用项目已有生成或边界校验工具，证明 FE、BE、Mock 确实消费该源；有生成物才加重生成一致性检查。
5. 完成一条真实业务切片的双端契约测试与实际联调，并回链现有 CASE/条款证据。
6. 更新本次涉及的分发/迁移、示例、测试和生成手册。
7. 只有试点暴露重复维护成本后，再评估可选 registry、反向索引、自动影响分析及其他协议适配器。

工具命令命名在蓝图和 CLI 设计时确定。优先扩展现有 `contracts check`；如新增模型诊断/生成子命令，应明确其 authoring 或正式 gate 语义，不让命令名暗示尚未具备的能力。

### 12.5 既有项目迁移与试点

先清点现有 OpenAPI、Schema、DTO、模型文档和重复字段，选定每类事实的真实源，再补引用与适配。不得在迁移时凭字段名称自动裁决语义冲突。

对于新规划批次采用新规则；既有冻结批次不得通过静默改文件或自动刷新指纹强制升级。明确采用的规则版本，在合法变更窗口升级并重新审查，迁移期间报告尚未覆盖的范围。

选一条包含正常、错误、空值和状态变化的真实前后端链路试点，完成“模型 → 协议 → 两端 → Mock/CT → 实际联调”的全链验证，再推广。避免先铺开所有文档、却没有一个真实消费者。

<a id="acceptance"></a>
## 13. 验收矩阵

以下均为待执行验收，不是已通过结果。

| 编号 | 场景 | 预期 |
|:---|:---|:---|
| A01 | FE/BE 对共同操作引用不同模型权威或基线 | 在合同/派发相应检查中发现，指出两端来源 |
| A02 | 模型只在磁盘修改、未提交 | 编辑期可诊断；正式阶段不能用它通过 |
| A03 | 同一稳定 ID 被两处声明为权威 | 报出重复权威，不能任选一个 |
| A04 | 正样例/Mock 返回非法枚举、错误金额单位或不允许的 null | 可机器表达的约束由校验拒绝；语义单位由明确规则及测试验证 |
| A05 | 结构负例或业务负例 | 前者按目标 Schema 约束拒绝；后者结构合法、在目标业务层拒绝，不混淆两类 |
| A06 | 修改模型但生成类型/客户端未更新 | 重生成比较或一致性检查发现 |
| A07 | TASK 必读链接缺失或执行先决条件成环 | 必需链接与现有 TASK DAG 分别检查；导航回链和递归数据模型不误报 |
| A08 | 新 Agent 只拿到 TASK 入口 | 可沿链接获得目标、范围、共同基线及完成标准，无需全库猜测 |
| A09 | 真实共享实现依赖未集成，或双方可独立生成 | 前者依赖检查指出缺口；后者允许并行，不强造基础任务 |
| A10 | 两个 worktree 对模型作无文本冲突但不兼容的修改 | 集成后的兼容检查和独立审查发现语义冲突 |
| A11 | 双方都符合模型，但模型违背 REQ | S5 独立审查或真实业务验证发现，不以生成 CT 全绿免责 |
| A12 | 取消订单遇到并发完成 | 协议、错误、UI 恢复及禁止副作用全部按同一承诺验证 |
| A13 | 模型变化触发 mutable evidence 刷新 | 冻结模型/subject 不被自动改指纹，进入合法变更路径 |
| A14 | 只改样式或只有单端变化 | 复用既有基线并明确适用性，不强造无关合同 |
| A15 | 用户切换根目录/worktree 阅读相同任务 | 相对链接可用，所用快照及权威归属清晰 |
| A16 | 从验收条款反查实现证据 | 可沿既有 AC/CASE/合同/TASK 链闭合，且不存在预填未来证据 |
| A17 | FE/BE checkout commit 不同，但共同契约闭包相同 | 判定设计基线一致，不因无关实现提交误报 |
| A18 | 非本次依赖的模型或可选背景存在历史坏链 | 不阻断本批次推进；诊断与必需输入缺失分开 |

结构/引用检查使用有代表性的正反 fixture；Git 输入和 worktree 依赖用隔离仓库测试；消费者验证使用真实试点实现。测试应验证实际缺陷，避免只验证字段存在或复述实现。

## 14. 实施进度与待细化项

| 工作 | 状态 |
|:---|:---|
| 当前根因与优化设计记录 | 本文已整理 |
| 新 L4 及相关蓝图修订 | 首版已完成 |
| 规范、模板与 skill 更新 | 首版已完成 |
| 解析、校验、基线及消费机制 | JSON Schema 首版已接线 |
| 试点、迁移和完整验收 | 隔离示例与回归验证；目标产品试点待实战 |

进入蓝图与实现时需要细化：试点原生契约及工具版本、稳定锚点约定、最小基线引用、已存在项目的迁移方式，以及模型文件闭包的登记方式。可选 registry、通用协议适配器和反向索引不是首版前提。共同数据与协议先于分端定稿、任务入口渐进披露的方向保持不变。

<a id="self-review"></a>
## 15. 逻辑连贯性与复杂度收益自审

### 15.1 结论与依据

核心优化值得实施：已定位的失效是双方独立解释数据和协议，权威源先行、实际消费者校验、任务精确导航都直接作用于根因。但原稿将完整扩展形态写得过于接近首版要求，存在把“减少猜测”做成“增加登记系统”的风险；不应按原稿全部能力一次性建设。

本次自审以[项目 L1 原则](blueprint/L1-design-principles.md)中的消费公理、成本公理、自然路径和三级强制阶梯为判据，并核对了现有 TASK 已有的文档清单及依赖 DAG。下面的收益判断是设计推断，尚无试点数据支持具体节省比例。

### 15.2 发现与修正

| 原稿问题 | 逻辑或成本风险 | 本次修正 |
|:---|:---|:---|
| 把模型 → SYNC 描述为严格线性 | 请求模型本身由操作塑造，可能先造无消费者的全局模型 | 模型与协议按业务切片共同收敛，先于分端定稿 |
| 三层模型容易被理解为三套文件 | 实现简单场景也要维护领域、DTO 和映射三份形式化定义 | 三层只划职责；同构引用，真实转换才记录 |
| registry、通用图、反向索引接近必选 | 与原生定义、CONTRACTS、TASK 和 runtime 重复记账 | 首版使用原生标识及已有索引；扩展按真实痛点引入 |
| 所有引用/阅读依赖都要求无环 | 合法递归数据和双向导航被误拦 | DAG 约束执行先决条件，阅读清单有限去重 |
| 负样例统一要求 Schema 拒绝 | 权限不足、状态冲突等合法请求的业务负例被错误分类 | 结构负例与业务负例分层验证 |
| “基础任务先行”可能全局串行化 | 独立生成的两端也被迫等待无必要的共享任务 | 仅对真实共享产物建立 TASK 依赖 |
| 同一基线可能被实现成同一 commit | 接收正常基础实现后，后派发 worker 被误报漂移 | 比较共同契约文件闭包，不比较整个 checkout SHA 是否相等 |
| 全库链接和模型检查范围不清 | 无关历史坏链导致本次任务不能推进，Hook 成本膨胀 | 必需输入闭包进入阶段检查，可选背景单独诊断 |
| 多语言生成与通用兼容分析同时建设 | 工厂先成为工具平台，真实联调问题迟迟未验证 | 单一试点协议，优先复用目标项目已有工具 |

### 15.3 首版投入与明确推迟项

| 机制 | 预期收益 | 相对成本 | 决策 |
|:---|:---|:---|:---|
| 权威模型与共同协议先行 | 直接减少两端猜字段、错误和空值行为 | 低至中，主要替换重复定义 | 首版必须 |
| TASK 精确链接、顺序与阅读目的 | 降低上下文拼接和漏读成本 | 低，扩展已有清单 | 首版必须 |
| 提交视图及冻结模型闭包 | 防止双方读到未提交或已漂移的设计 | 中，复用现有基线体系 | 首版必须 |
| 模型样例校验及两端实际消费验证 | 把偏移发现时间提前到真实联调之前 | 中，使用试点已有工具 | 首版必须 |
| 真实联调与独立语义审查 | 避免共同源本身错误带来虚假一致 | 已有责任，需补输入 | 保留并接线 |
| 自定义 registry / 通用关系图 | 大型异构项目可能受益 | 中至高，新增元模型及迁移成本 | 首版不建 |
| 持久化反向索引 / 自动全量影响分析 | 消费者多时减少人工追踪 | 中至高，有漏报与失效风险 | 先查询既有引用，后续按需要引入 |
| 多协议生成框架 / 通用映射 DSL | 潜在复用收益 | 高，工具维护成为新职责 | 不作为本次落地范围 |
| 自动判断任意语义兼容性 | 理想收益高 | 无法可靠承诺 | 不承诺；工具差异加独立审查 |

“首版必须”指试点的完整交付责任，不代表每项都增加一个新 gate 或新的登记字段。首版只增加对已命名失效的确定性检查；控制状态仍由既有 runtime 拥有。

### 15.4 最小完整闭环

选一条实际操作及其正常、结构错误和业务拒绝场景：

1. 在已有源设计下完善一份机器契约及必要的 SYNC 行为说明；删除对应重复定义。
2. FE/BE 合同引用同一操作与模型；TASK 按角色提供有限的精确阅读链接。
3. 正式检查以已提交文件闭包为输入，沿既有 S3/S5 路径登记与冻结。
4. 两端通过已有生成器或边界校验器消费共同源，Mock 也用它校验；不能只检查 model_ref 文本。
5. 实际联调验证状态、权限、错误恢复和副作用，再沿已有 CASE/条款回链。

这条链必须完整，不能把首版缩成只有文档和引用校验；但无需先完成全项目迁移、注册中心或通用代码生成平台。

### 15.5 如何判定收益，而不是凭感觉增加机制

试点记录以下事实，优先从已有 TASK、finding 和命令证据提取，不新增每日手填报表：

- 共同模型相关缺陷首次在哪个阶段暴露；有多少直到真实联调才发现。
- 同一字段/操作变化需要改多少份独立人工定义，哪些改动只是生成物。
- 因契约偏差发生的返工轮次，以及修正涉及的两端任务。
- 新上下文 Agent 是否能仅从 TASK 入口找到必需定义；是否仍需人工补充文件位置或语义。
- 新增校验耗时、误报、无关变更触发次数，以及维护工具/登记的实际工作量。

可与同项目可比的历史任务对照；历史数据不足时，先验证 A01—A18 中的具体失效能否被正确识别，不能伪造效率提升百分比。单个试点只能验证可行性和已覆盖失效，不能证明所有项目都能获得相同收益。

可推广的条件是：重复权威定义减少、实际双端确实同源、已知结构偏差能提前暴露、角色阅读入口可用，且没有因无关文件或正常提交差异产生阻塞。若主要工作变成修登记、修生成框架或处理误报，则继续削减外围机制，优先保留共同源和真实消费链，而不是继续增加校验。

## 16. 参考与前序设计

- [前序 Worktree / Evidence / Hook 整改](L4-worktree-hook-remediation.md)：开发分支、提交视图、回收和 evidence 哈希边界。
- [现有模块场景规则](docs/rules/scenario-model.md)：当前真相唯一、REQ 不拥有私有副本及 fixture 边界。
- [JSON Schema object](https://json-schema.org/understanding-json-schema/reference/object)：required 与 null/缺失的区别。
- [OpenAPI 规范](https://spec.openapis.org/oas/latest.html)：操作、请求响应与可复用结构。实现时核实所选版本、Schema 方言及生成器实际支持，不能仅追随 latest URL 让构建输入漂移。


## 17. 首版交付与验证记录（2026-09-19）

### 17.1 正式设计与安装规范

- 新增 [L4 共享模型与合同治理](blueprint/L4-shared-model-contract-governance.md)，同步 L2、L3 阶段及控制面/派发设计。正式机制以蓝图为权威。
- 新增 [共享模型规则](docs/rules/shared-model-contracts.md)，更新 MODEL、CONTRACTS、FE、BE、SYNC、TASK 模板、Agent 协议及规划/派发 skill。
- TASK 以有目的的有序相对链接进入合同/协议/模型；合同不再要求回填未来任务。共享模型与 SYNC 共同收敛，再派生分端责任。

### 17.2 实际实现

- [共享输入检查](internal/sharedmodel/check.go)：复用索引表和原生 JSON Schema 引用，不引入 registry。验证消费者引用、操作/数据位置唯一性、Schema、正样例与结构负例，保留业务负例语义。
- 形式化输入闭包由实际本地 Schema 加载及样例组成，拒绝网络加载、越界和 worktree 输入；正式检查要求 git_tree，磁盘 authoring 检查不授予阶段资格。
- S3 按绑定 REQ 选择新增模型检查的索引，原有条款检查保留原范围。编辑期 CLI 默认检查所有显式采用策略的索引；未采用的旧索引输出 legacy 诊断。
- [登记与冻结](internal/transition/shared_models.go)：复用 design documents 和 S5 subjects；冻结时拒绝内容漂移、新增未审输入及静默缩小闭包。合法返回 S3 可以重新登记当前代集合。
- [任务检查](internal/semantic/tasks.go)：linked-v1 清单检查必要文件、锚点、模式和重复链接；继续复用 TASK 执行 DAG，不把导航或递归模型判为依赖环。
- 共享 Schema/样例参与 S5，但在 S2 架构交付查找中被明确排除，避免新增 design 文档意外替代架构产物。
- `contracts check --json` 在发现问题时返回非零状态，不再只输出 problems 却返回成功。

### 17.3 已执行的验证范围

隔离示例入口见 [共享模型试点](docs/examples/shared-model/README.md)。该示例的合同 CLI 检查通过；HTTP 消费者/提供者测试验证正常取消、结构拒绝、业务拒绝不修改状态，以及 Mock 枚举偏移。它是工厂内部的合成集成测试，不是目标产品的浏览器联调证据。

针对性测试覆盖：同源引用与偏移、本地递归和传递依赖、远程/越界输入、必读坏链、非法阅读模式、绑定 REQ 范围、已提交快照、设计冻结与重登记、S3 自然 guard、S2/S5 文档职责隔离、JSON CLI 失败退出码。

完整回归和 doctor 的最终结果在本节末尾记录，不能把首次失败或单包测试当成全量通过。

### 17.4 明确未宣称实现的能力

- 首版原生适配 JSON Schema；没有实现任意 OpenAPI/Proto 的解析、兼容性分析或多语言代码生成平台。
- 旧合同不自动迁移，不伪造新策略验证结果；新规划通过模板与 S5 审查明确采用范围。
- 条件阅读的触发语义、跨端行为等仍由 Agent 和独立审查判断，不由字符串匹配猜测。
- 没有目标业务项目可用于生产试点，本次未声称测得真实返工下降比例；后续按第 15 节指标实战回灌。


### 17.5 最终验证结果

- `go test -p 2 ./...`：完整通过。
- 新增检查、S3 guard、冻结、JSON CLI 失败退出码及 HTTP 消费者示例的针对性测试通过。
- `doctor --root .`：结构、示例、语义链接及手册同步检查通过；其运行时健康指标独立呈现，不将历史指标归零视为本次交付条件。
- `git diff --check` 通过；新增机制文档、报告和示例中的 80 个具体本地文件链接无缺失。
- 回归过程曾遇到工作区其他恢复测试的编译错误，后续已消失；高并行全量执行曾出现一次既有 S11 用例未推进，单例连续三次及最终低并行全量均通过。未据此修改发布语义，也未宣称已经定位该次非复现失败的根因。

本次改动保留在工作区，未自动 commit 或 release。目标业务项目中的真实双端/浏览器试点仍需在采用该机制的具体 REQ 中完成。
